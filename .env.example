# =============================================================================
# NDI Compliance System - Environment Variables
# نظام الامتثال لمؤشر البيانات الوطني - متغيرات البيئة
# =============================================================================

# =============================================================================
# Database / قاعدة البيانات
# =============================================================================
POSTGRES_USER=ndi_user
POSTGRES_PASSWORD=ndi_password
POSTGRES_DB=ndi_db
POSTGRES_PORT=5432

# Full database URL (used by backend)
# For Docker internal: postgresql://ndi_user:ndi_password@postgres:5432/ndi_db
# For external DB: postgresql://ndi_user:ndi_password@192.168.1.102:5432/ndi_db
DATABASE_URL=postgresql://ndi_user:ndi_password@postgres:5432/ndi_db

# =============================================================================
# Redis / ذاكرة التخزين المؤقت
# =============================================================================
# For Docker internal: redis://redis:6379
# For external: redis://192.168.1.102:6379
REDIS_URL=redis://redis:6379
REDIS_PORT=6379

# =============================================================================
# Qdrant Vector Database / قاعدة بيانات المتجهات
# =============================================================================
# For Docker internal: http://qdrant:6333
# For external: http://192.168.1.102:6333
QDRANT_URL=http://qdrant:6333
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334

# =============================================================================
# Application / التطبيق
# =============================================================================
APP_PORT=80
APP_ENV=production
DEBUG=false
SECRET_KEY=change-this-to-a-secure-random-string
ENCRYPTION_KEY=change-this-to-a-32-byte-key-for-encryption

# =============================================================================
# Authentication / المصادقة
# =============================================================================
NEXTAUTH_URL=http://localhost
NEXTAUTH_SECRET=change-this-to-a-secure-random-string

# =============================================================================
# AI Providers / مزودي الذكاء الاصطناعي
# =============================================================================
# NOTE: AI API keys are now managed via the Settings UI (/settings)
# ملاحظة: يتم الآن إدارة مفاتيح API للذكاء الاصطناعي عبر واجهة الإعدادات
#
# Supported providers / المزودون المدعومون:
# - OpenAI (GPT-4, GPT-4-turbo, GPT-3.5-turbo)
# - Claude/Anthropic (claude-3-opus, claude-3-sonnet, claude-3-haiku)
# - Google Gemini (gemini-pro, gemini-pro-vision)
# - Azure OpenAI (with custom endpoint)
#
# Configure these in the app at: /settings
# قم بتكوينها في التطبيق على: /settings

# =============================================================================
# Storage (Optional) / التخزين (اختياري)
# =============================================================================
# S3-compatible storage for evidence files
# تخزين متوافق مع S3 لملفات الشواهد
# S3_BUCKET=ndi-evidence
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_ENDPOINT=https://s3.amazonaws.com

# =============================================================================
# CORS / مشاركة الموارد عبر الأصول
# =============================================================================
ALLOWED_ORIGINS=http://localhost,http://localhost:80

# =============================================================================
# Generate Secure Keys / إنشاء مفاتيح آمنة
# =============================================================================
# Run these commands to generate secure keys:
# openssl rand -hex 32          # For SECRET_KEY
# openssl rand -base64 32       # For NEXTAUTH_SECRET
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"  # For ENCRYPTION_KEY
